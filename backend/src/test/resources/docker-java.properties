# Docker Engine 29+ (Docker Desktop 4.52+) requires API >= 1.44.
# docker-java in Testcontainers 1.20.x otherwise negotiates an older version and
# Windows named-pipe access fails with a stub HTTP 400 from docker_cli.
api.version=1.44
